name: pyogrio-dev
channels:
  - conda-forge
dependencies:
  # Required
  - numpy
  - libgdal-core
  - shapely>=2
  # Optional
  - geopandas-base
  - pyproj
  - pyarrow
  # Specific for dev
  - cython
  - pre-commit
  - pytest
  - ruff==0.5.2
  - versioneer
