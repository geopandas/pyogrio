pytest
# dependencies of geopandas (installed separately with --no-deps to avoid fiona)
pandas
pyproj
# Windows ARM64 wheels are currently available from the scientific Python nightly index.
shapely>=2; sys_platform != "win32" or platform_machine != "ARM64"
packaging
# optional test dependencies
# PyArrow does not currently publish Windows ARM64 wheels.
pyarrow; sys_platform != "win32" or platform_machine != "ARM64"
