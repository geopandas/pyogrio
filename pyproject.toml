[build-system]
# numpy and Cython are required to execute setup.py to build Cython extensions
requires = [
    "setuptools",
    "wheel",
    "Cython>=0.29",
    "oldest-supported-numpy",
]

[tool.cibuildwheel]
skip = ["cp36-*", "cp37-*", "pp*"]
archs = ["auto64"]

[tool.cibuildwheel.linux.environment]
GDAL_INCLUDE_PATH = "/vcpkg/packages/gdal_x64-linux/include"
GDAL_LIBRARY_PATH = "/vcpkg/packages/gdal_x64-linux/lib"
GDAL_DATA = "/vcpkg/packages/gdal_x64-linux/share/gdal"
PROJ_LIB = "/vcpkg/packages/proj4_x64-linux/share/proj4"